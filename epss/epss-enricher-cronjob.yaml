apiVersion: batch/v1
kind: CronJob
metadata:
  name: epss-enricher
  namespace: app
spec:
  schedule: "30 3 * * *"  # 毎日 03:30 UTC (cve-watch 03:00 の後)
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 300
      completions: 1
      parallelism: 1
      backoffLimit: 1
      template:
        spec:
          imagePullSecrets:
          - name: ghcr-secret
          containers:
          - name: epss-enricher
            image: ghcr.io/kamonabe/mitre-python:1.0.0
            imagePullPolicy: IfNotPresent
            command:
              - python
              - /app/epss_enricher.py
            securityContext:
              runAsNonRoot: true
              runAsUser: 1000
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop: ["ALL"]
            env:
            - name: PYTHONDONTWRITEBYTECODE
              value: "1"
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: epss-db
                  key: host
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: epss-db
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: epss-db
                  key: password
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: epss-db
                  key: database
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
              limits:
                cpu: 200m
                memory: 256Mi
            volumeMounts:
            - name: script
              mountPath: /app
            - name: common
              mountPath: /common
          volumes:
          - name: script
            configMap:
              name: epss-enricher-script
          - name: common
            configMap:
              name: common-lib
          restartPolicy: Never
